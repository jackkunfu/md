# H5

## svg/canvas

```
Canvas 和 SVG 都允许您在浏览器中创建图形，但是它们在根本上是不同的。

Canvas:
  Canvas 是逐像素进行渲染的。

SVG:
  是一种使用 XML 描述 2D 图形的语言。
  基于 XML，可以为某个元素附加 JavaScript 事件处理器
  每个被绘制的图形均被视为对象。如果 SVG 对象的属性发生变化，那么浏览器能够自动重现图形。
```
